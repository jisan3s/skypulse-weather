services:
  - type: web
    runtime: static
    name: skypulse-weather
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/skypulse-weather
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
